#ifndef ROW_DETAIL_H
#define ROW_DETAIL_H

/**
 * Show a popup window with full row detail view.
 * Displays column names on the left and full cell values on the right
 * with word wrapping for long text.
 *
 * @param real_row  The actual row index in the rows[] array
 */
void show_row_detail(int real_row);

#endif
